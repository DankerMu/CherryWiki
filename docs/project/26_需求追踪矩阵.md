# 26. 需求 → API → Schema → 测试追踪矩阵

## 1. 目的

确保每个功能需求都有对应的 API 端点、数据表、测试用例和 Phase 归属。任何一列为空代表有缺口需要补齐。

## 2. Phase 1 追踪

| 需求 | API | Schema | 测试 | 验收点 |
|---|---|---|---|---|
| 用户登录认证 | POST /api/auth/login, /refresh | users, sessions | P1-E1 前置 | 登录后获得 JWT |
| 创建 Space | POST /api/spaces | spaces, space_permissions | P1-E1 前置 | Space 可见可配置 |
| 上传资料归档 | POST /api/spaces/{id}/uploads | file_blobs, source_documents | P1-E1 step1 | 文件进入 archive |
| 文件去重 | POST /api/spaces/{id}/uploads | file_blobs (SHA256 unique) | P1-E6 | 重复文件返回已有 ID |
| 文件解析 | — (内部 ingestion job) | jobs, source_documents | P1-E1 step2 | status=parsed |
| Graphify 运行 | POST /api/spaces/{id}/graphify/runs | graphify_runs, jobs | P1-E1 step3 | run completed |
| Graphify 失败回滚 | — (内部逻辑) | graphify_runs, index_snapshots | P1-E4 | active_snapshot 不变 |
| Graphify 输出保护 | — (内部逻辑) | graphify_runs (quarantine) | §4.2 安全测试 | 异常输出不激活 |
| Canonical Wiki Repo 写入 | — (内部 worker) | wiki_pages, wiki_page_versions | P1-E1 step3 | Repo main 有页面 |
| Wiki 页面浏览 | GET /api/spaces/{id}/wiki/pages/{pid} | wiki_pages, wiki_sections | P1-E1 step4 | Cherry Web 可见 |
| Wiki 页面内容 | GET /api/spaces/{id}/wiki/pages/{pid}/content | wiki_page_versions | P1-E1 step4 | Markdown 可读 |
| Wiki 发布 | POST /api/spaces/{id}/wiki/pages/{pid}/publish | wiki_page_versions | P1-E2 step5 | status=published |
| 索引构建 | — (内部 indexer) | wiki_chunks, embeddings, index_snapshots | P1-E2 | index_status=indexed |
| Chat 引用 Wiki | POST /api/chat/completions | chat_messages, answer_citations | P1-E2 step6 | citations 非空 |
| Chat 只引用 Published Wiki | POST /api/chat/completions | wiki_chunks (index_snapshot) | P1-E5 | 未发布内容不返回 |
| Chat 无知识降级 | POST /api/chat/completions | chat_messages | P1-E5 + §4.1 | strict=no_hit / 宽松=model_knowledge |
| Source Document 不直接检索 | POST /api/chat/completions | N/A（行为约束） | P1-E5 | 未 Graphify 的资料不在引用中 |
| 权限隔离 | 所有 API | space_permissions, acl_json | P1-E3 | 无权限用户看不到内容 |
| 撤权即时生效 | — (缓存机制) | permission_versions | §4.1 安全测试 | 5s 内失效 |
| Job 查看/取消 | GET/POST /api/jobs/{id} | jobs | P1-E9 | 用户可查看可取消 |
| 模型管理 | GET/POST /api/admin/models | model_configs | Admin 验收 | 可添加可测试 |
| 审计日志 | GET /api/admin/audit-logs | audit_logs | 权限测试 | 关键操作有记录 |
| 系统健康 | GET /api/admin/system/health | — | P1 部署验收 | 所有组件 healthy |
| SSRF 防护 | POST /api/spaces/{id}/uploads (URL) | — | §4.3 安全测试 | 内网地址被拒 |
| Prompt injection 防护 | — (ingestion scan) | wiki_chunks (injection_risk) | §4.5 安全测试 | Chat 不执行注入指令 |

## 3. Phase 2 追踪

| 需求 | API | Schema | 测试 | 验收点 |
|---|---|---|---|---|
| Docmost 页面导入 | PUT /internal/bridge/pages/{id}/import | wiki_pages, wiki_page_versions | P2-E1 | Docmost 可编辑 |
| Docmost 保存回写 Repo | POST /internal/docmost/events/page-saved | bridge_events, wiki_page_versions | P2-E2 | Repo 新 commit |
| Bridge webhook 幂等 | POST /internal/docmost/events/* | bridge_events (event_id unique) | P2-E5, §4.4 | 重复不产生新版本 |
| Bridge HMAC 校验 | — (Guard) | — | P2-E6 | 错误签名被拒 |
| 人工区块保护 | — (merge logic) | page_block_metadata | P2-E8 | human 区块不被覆盖 |
| 候选更新审核 | POST /api/.../proposals/{id}/accept | wiki_update_proposals | P2-E9 | 管理员可接受/拒绝 |
| 权限映射同步 | PUT /internal/bridge/spaces/{id}/permissions | permission_versions | P2-E7 | Docmost 权限与 Cherry 一致 |
| Docmost 同步状态 | GET /internal/bridge/spaces/{id}/sync-status | — | P2 部署验收 | status=healthy |

## 4. Phase 3 追踪

| 需求 | API | Schema | 测试 | 验收点 |
|---|---|---|---|---|
| Graph 节点搜索 | GET /api/graph/nodes | graph_nodes | P3-E1 | 搜索返回节点 |
| Graph 路径解释 | POST /api/graph/path | graph_edges, graph_nodes | P3-E2 | 路径可视化 |
| INFERRED/AMBIGUOUS 策略 | POST /api/chat/completions | graph_edges (confidence) | P3-E3, P3-E4 | 标注正确 |
| Source chain 追溯 | POST /api/chat/completions | wiki_chunks (source_chain), answer_citations | P3-E7 | 引用可展开到源 |
| Retrieval trace | GET /api/admin/retrieval-traces/{id} | retrieval_traces | P3-E5 | 管理员可查 |
| Graph path ACL | POST /api/graph/path | graph_nodes (acl_json) | P3-E6 | 无权限路径不返回 |
| Graph evidence refs | — (internal) | graph_evidence_refs | — | 反向查询可用 |
| Model usage tracking | — (internal) | model_usage_logs | — | token 计量准确 |

## 5. Phase 4 追踪

| 需求 | API | Schema | 测试 | 验收点 |
|---|---|---|---|---|
| 低置信审核 | GET /api/admin/... | graph_edges, feedback_items | P4-E1 | 队列可审核 |
| 知识反馈闭环 | POST feedback → edit → reindex | feedback_items | P4-E3 | 新版本被 Chat 使用 |
| MCP Gateway | POST /api/mcp/... | api_tokens | P4-E4 | Agent 可调用且遵守权限 |
| 备份恢复 | — (ops) | 全部 | P4-E5 | 恢复后系统正常 |

## 6. 使用说明

- 新增需求时必须在对应 Phase 表格中添加一行
- API 列为空 = 需要设计端点
- Schema 列为空 = 需要确认数据模型
- 测试列为空 = 需要编写测试用例
- 每次 Phase 交付前审查本矩阵，确保无空格
