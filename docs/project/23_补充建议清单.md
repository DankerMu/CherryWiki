# 23. 补充建议清单

本清单是在 `todo.md` 基础上的补充，已合并到对应文档、schema 或路线图中。

## 1. 上传与解析安全

| 建议 | 已合并位置 |
|---|---|
| 文件解析放入沙箱容器，限制 CPU、内存、网络和文件系统。 | `17_风险清单与决策记录.md` R12、`14_测试验收规范.md` 安全测试。 |
| URL/网页抓取增加 SSRF 防护：协议 allowlist、私网 IP 拦截、大小限制、超时。 | `17_风险清单与决策记录.md` R13。 |
| ZIP 解压采用路径穿越检测和总大小限制。 | `14_测试验收规范.md`、`17_风险清单与决策记录.md`。 |

## 2. 可观测性

| 建议 | 已合并位置 |
|---|---|
| 每个 Graphify run、index job、chat retrieval 生成 trace_id。 | `16_实施路线图与里程碑.md` Phase 4、`11_API规范.md` retrieval traces。 |
| 记录检索候选、rerank 分数、ACL 过滤原因。 | `10_数据模型与数据库设计.md` `retrieval_traces` 建议表。 |
| Worker 指标：成功率、失败率、耗时、队列长度。 | `15_部署运维规范.md` 后续应细化。 |

## 3. 数据治理

| 建议 | 已合并位置 |
|---|---|
| 建立 `wiki_update_proposals` 表，承载候选更新。 | `10_数据模型与数据库设计.md`。 |
| 建立 `feedback_items` 表，把用户反馈进入知识治理。 | `10_数据模型与数据库设计.md`。 |
| 低置信关系审核与重复页面合并建议进入 Phase 4。 | `16_实施路线图与里程碑.md`。 |

## 4. 发布与回滚

| 建议 | 已合并位置 |
|---|---|
| Chat 永远读取最近一次成功索引版本。 | `08_强耦合设计_六层.md` 一致性保障。 |
| Graphify schema 不兼容时不覆盖上一成功索引。 | `21_Graphify_输出Schema契约.md`。 |
| Docmost rebase 失败时进入只读维护模式。 | `22_Docmost_Fork_改动清单.md`。 |

## 5. 后续建议待补文档

这些建议未强制进入 v0.2，但建议后续新增文档：

| 建议文档 | 内容 |
|---|---|
| `24_观测与告警设计.md` | Metrics、logs、traces、alerts、dashboard。 |
| `25_备份恢复演练手册.md` | PostgreSQL、MinIO、Wiki Repo、Graphify output 恢复流程。 |
| `26_安全威胁模型.md` | 上传、SSRF、权限、GraphRAG、Docmost Bridge、MCP Gateway 威胁建模。 |
| `27_本地代码审计脚本.md` | 自动统计 Cherry Studio 复用等级、LOC、Electron API 命中。 |
